# The service is bound to the private Tailscale address by default.
BUCRATER_SERVER_HOST=100.74.38.58
BUCRATER_SERVER_PORT=8092
BUCRATER_SERVER_DATABASE=/var/lib/bucrater-server/bucrater.db

# Optional. When set, clients must send Authorization: Bearer <token>.
# BUCRATER_SERVER_TOKEN=generate-a-long-random-token
